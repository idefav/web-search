# camofox-web-search-pi

Native Pi extension that registers `web_search` and `web_fetch` and calls the shared authenticated REST gateway.

```bash
pi install npm:camofox-web-search-pi
```

Set `WEB_SEARCH_API_KEY`; configure the endpoint with the `camofox-web-search` installer or `WEB_SEARCH_ENDPOINT`.
